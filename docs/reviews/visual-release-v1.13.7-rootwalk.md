# Frontier Survival v1.13.7 — Lantern Rootwalk checkpoint

Decision: PUBLISHED INCREMENTAL CHECKPOINT — VISUAL FOLLOW-UP REQUIRED

## Product slice

Added an authored Mangrove destination prop at the deterministic lagoon anchor `(55,58)`.

- five-block plank rootwalk;
- paired mangrove-log posts;
- mangrove-leaf canopy cap;
- central torch lantern;
- root accents beside the span;
- mirrored synchronous and chunk-worker placement;
- no save, combat, fauna, or unrelated UI changes.

## Provenance

- Product commit: `1b47164a4de322a1f3e9caa3661c277d4a472280`
- Tag: `v1.13.7`
- Base: v1.13.6 / `2dba390`
- Cache chain: `main.js?v=469` → `game.js?v=458` → `world.js?v=427` → `chunk-worker.js?v=288`
- Fixed seed: `1884808540`

## Evidence

- Full smoke: PASS.
- Syntax checks: PASS.
- `git diff --check`: PASS.
- Root/public parity: PASS.
- Import audit: 125 edges, 0 missing targets, 0 missing cache-bust queries.
- Local Start/runtime reached `started=true`, hidden title, `biome=mangrove`, and zero page-owned errors.
- Runtime block probe confirmed plank/torch destination blocks at the authored anchor.

## Visual status

The accepted v1.13.6 open-water approach composition remains the baseline visual proof. The new Rootwalk is implemented and runtime-reachable, but the water-side close-up screenshot timed out during this pass and the direct approach frame was too close to foreground geometry. Therefore the Rootwalk is not visually accepted as Wow-level yet.

This release is intentionally labeled an incremental/unverified prop checkpoint. No AAA parity claim is made.

## Next bounded slice

Capture a stable water-side Rootwalk frame from an ordinary player-height approach. If the bridge remains occluded, move the prop one or two cells toward the open-water corridor rather than adding more geometry. Require a clear torch, posts, root accents, water/horizon, and zero HUD/world overlap before calling the prop accepted.
