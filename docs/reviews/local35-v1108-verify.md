# v1.10.8 Verification

## Title Check
The `v1.10.8` commit title is **"v1.10.8: ..."** as per the task.

## Hook Confirmations
- `_tryShootBow( who )`: Present in `game.js` and correctly forwards to bow logic.
- `respawn(who)`: Called during respawn flow; ensures player state reset.

No source edits performed. Only documentation created.